# LIBRARY_MANAGEMENT_SYSTEM
An end-to-end Library Management ETL system built with Python, Pandas, and MySQL that extracts transaction data, performs transformations (due dates, late days, fines), and loads it into a structured database with query support.
